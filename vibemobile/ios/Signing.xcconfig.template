// Copy this file to Signing.xcconfig and fill in your values.
// Signing.xcconfig is gitignored — never commit it.
//
// Find your Team ID at: https://developer.apple.com/account → Membership
// A free Apple ID works for sideloading (7-day expiry, re-sign with: make ios-resign)

DEVELOPMENT_TEAM = XXXXXXXXXX
// Optional: override the bundle ID if your free account needs a unique one
// PRODUCT_BUNDLE_IDENTIFIER = com.yourname.vibecodyMobile
