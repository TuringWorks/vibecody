diff --git a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/diffEditorViewZones.js b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/diffEditorViewZones.js
index cbc33d8..bd299d1 100644
--- a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/diffEditorViewZones.js
+++ b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/components/diffEditorViewZones/diffEditorViewZones.js
@@ -141,15 +141,20 @@ let DiffEditorViewZones = class DiffEditorViewZones extends Disposable {
             const deletedCodeLineBreaksComputer = !renderSideBySide ? this._editors.modified._getViewModel()?.createLineBreaksComputer() : undefined;
             if (deletedCodeLineBreaksComputer) {
                 const originalModel = this._editors.original.getModel();
+                const originalModelLineCount = originalModel.getLineCount();
+                // Validate ALL alignment ranges before starting any computation.
+                // If any range is out of bounds the diff model is stale (an edit arrived
+                // before the diff could be recomputed). Bail out entirely so we don't
+                // produce a partial view-zone set that leaves the editor in an inconsistent
+                // visual state. The diff will be recomputed on the next content-change tick.
+                for (const a of alignmentsVal) {
+                    if (a.diff && a.originalRange.endLineNumberExclusive - 1 > originalModelLineCount) {
+                        return { orig: origViewZones, mod: modViewZones };
+                    }
+                }
                 for (const a of alignmentsVal) {
                     if (a.diff) {
                         for (let i = a.originalRange.startLineNumber; i < a.originalRange.endLineNumberExclusive; i++) {
-                            // `i` can be out of bound when the diff has not been updated yet.
-                            // In this case, we do an early return.
-                            // TODO@hediet: Fix this by applying the edit directly to the diff model, so that the diff is always valid.
-                            if (i > originalModel.getLineCount()) {
-                                return { orig: origViewZones, mod: modViewZones };
-                            }
                             deletedCodeLineBreaksComputer?.addRequest(originalModel.getLineContent(i), null, null);
                         }
                     }
@@ -169,9 +174,9 @@ let DiffEditorViewZones = class DiffEditorViewZones extends Disposable {
                         const deletedCodeDomNode = document.createElement('div');
                         deletedCodeDomNode.classList.add('view-lines', 'line-delete', 'line-delete-selectable', 'monaco-mouse-cursor-text');
                         const originalModel = this._editors.original.getModel();
-                        // `a.originalRange` can be out of bound when the diff has not been updated yet.
-                        // In this case, we do an early return.
-                        // TODO@hediet: Fix this by applying the edit directly to the diff model, so that the diff is always valid.
+                        // Guard: in side-by-side mode the pre-validation block above is
+                        // skipped (no lineBreaksComputer), so we still need to check bounds
+                        // here before accessing model content.
                         if (a.originalRange.endLineNumberExclusive - 1 > originalModel.getLineCount()) {
                             return { orig: origViewZones, mod: modViewZones };
                         }
diff --git a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorViewModel.js b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorViewModel.js
index 399dbc8..724efad 100644
--- a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorViewModel.js
+++ b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorViewModel.js
@@ -1,5 +1,6 @@
 import { RunOnceScheduler } from '../../../../base/common/async.js';
 import { CancellationTokenSource } from '../../../../base/common/cancellation.js';
+import { onUnexpectedError } from '../../../../base/common/errors.js';
 import { Disposable, toDisposable } from '../../../../base/common/lifecycle.js';
 import '../../../../base/common/observableInternal/index.js';
 import { IDiffProviderFactoryService } from './diffProviderFactoryService.js';
@@ -205,16 +206,31 @@ let DiffEditorViewModel = class DiffEditorViewModel extends Disposable {
                 const edits = TextEditInfo.fromModelContentChanges(e.changes);
                 modifiedTextEditInfos = combineTextEditInfos(modifiedTextEditInfos, edits);
             }));
-            let result = await documentDiffProvider.diffProvider.computeDiff(model.original, model.modified, {
-                ignoreTrimWhitespace: this._options.ignoreTrimWhitespace.read(reader),
-                maxComputationTimeMs: this._options.maxComputationTimeMs.read(reader),
-                computeMoves: this._options.showMoves.read(reader),
-            }, this._cancellationTokenSource.token);
+            let result;
+            try {
+                result = await documentDiffProvider.diffProvider.computeDiff(model.original, model.modified, {
+                    ignoreTrimWhitespace: this._options.ignoreTrimWhitespace.read(reader),
+                    maxComputationTimeMs: this._options.maxComputationTimeMs.read(reader),
+                    computeMoves: this._options.showMoves.read(reader),
+                }, this._cancellationTokenSource.token);
+            } catch (e) {
+                // Diff computation failed (e.g. worker OOM or crash on a very large diff).
+                // Log it but do not propagate — an unhandled rejection here would crash the
+                // entire editor. Leave isDiffUpToDate=false so the diff retries on the next
+                // content change.
+                onUnexpectedError(e);
+                return;
+            }
             if (this._cancellationTokenSource.token.isCancellationRequested) {
                 return;
             }
             if (model.original.isDisposed() || model.modified.isDisposed()) {
-                // TODO@hediet fishy?
+                // Models were disposed while the async diff was in flight (e.g. the user
+                // switched away from the diff editor). Reset state cleanly.
+                transaction(tx => {
+                    this._diff.set(undefined, tx);
+                    this._isDiffUpToDate.set(true, tx);
+                });
                 return;
             }
             result = normalizeDocumentDiff(result, model.original, model.modified);
diff --git a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorWidget.js b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorWidget.js
index 0593390..6008de6 100644
--- a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorWidget.js
+++ b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffEditorWidget.js
@@ -1,6 +1,6 @@
 import { h, getWindow } from '../../../../base/browser/dom.js';
 import { findLast } from '../../../../base/common/arraysFind.js';
-import { onUnexpectedError, BugIndicatingError } from '../../../../base/common/errors.js';
+import { onUnexpectedError } from '../../../../base/common/errors.js';
 import { Event } from '../../../../base/common/event.js';
 import { readHotReloadableExport } from '../../../../base/common/hotReloadHelpers.js';
 import { toDisposable } from '../../../../base/common/lifecycle.js';
@@ -236,8 +236,10 @@ let DiffEditorWidget = class DiffEditorWidget extends DelegatingEditor {
                 return;
             }
             for (const m of [model.model.original, model.model.modified]) {
-                store.add(m.onWillDispose(e => {
-                    onUnexpectedError(new BugIndicatingError('TextModel got disposed before DiffEditorWidget model got reset'));
+                store.add(m.onWillDispose(() => {
+                    // The text model was disposed while still referenced by the diff editor.
+                    // This happens when the caller disposes models before calling setModel(null).
+                    // Reset gracefully instead of crashing.
                     this.setModel(null);
                 }));
             }
@@ -317,11 +319,14 @@ let DiffEditorWidget = class DiffEditorWidget extends DelegatingEditor {
                 });
                 const prevValueRef = this._diffModelSrc.get()?.createNewRef(this);
                 this._diffModelSrc.set(viewModel?.createNewRef(this), tx);
-                setTimeout(() => {
-                    // async, so that this runs after the transaction finished.
-                    // TODO: use the transaction to schedule disposal
+                // Defer disposal until after the transaction has fully propagated so that
+                // reactive subscribers still see a valid model during the current tick.
+                // Use queueMicrotask (runs before the next render frame) instead of
+                // setTimeout(0) (runs after I/O callbacks) to reduce the window in which
+                // the old ref is alive but no longer the active model.
+                queueMicrotask(() => {
                     prevValueRef?.dispose();
-                }, 0);
+                });
             });
         }
     }
diff --git a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffProviderFactoryService.js b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffProviderFactoryService.js
index a0304e6..2c51098 100644
--- a/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffProviderFactoryService.js
+++ b/node_modules/monaco-editor/esm/vs/editor/browser/widget/diffEditor/diffProviderFactoryService.js
@@ -107,7 +107,16 @@ let WorkerBasedDocumentDiffProvider = class WorkerBasedDocumentDiffProvider {
             };
         }
         if (!result) {
-            throw new Error('no diff result available');
+            // The worker returned null — this typically means the worker process crashed
+            // or ran out of memory computing a very large diff. Return a safe empty result
+            // with quitEarly=true so the UI stays stable and the diff can be retried when
+            // the model changes next.
+            return {
+                changes: [],
+                identical: false,
+                quitEarly: true,
+                moves: [],
+            };
         }
         // max 10 items in cache
         if (WorkerBasedDocumentDiffProvider_1.diffCache.size > 10) {
